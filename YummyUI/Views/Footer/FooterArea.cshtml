@using YummyUI.DTOs.FooterDTOs
@model List<ResultFooterDto>
@{
    Layout = "_AdminLayout";
}

<style>
  .ai-contact-container,
  .ai-contact-container * {
    box-sizing: border-box;
  }

  .ai-contact-container {
    height: 100%;
    min-height: 0;
    padding: 1.6rem 2rem;
    position: relative;
    overflow: hidden;
    display: flex;
    flex-direction: column;

    background:
      radial-gradient(1200px 600px at 70% 80%, rgba(99, 102, 241, 0.15), transparent 60%),
      radial-gradient(900px 500px at 15% 20%, rgba(139, 92, 246, 0.18), transparent 60%),
      radial-gradient(800px 420px at 90% 10%, rgba(79, 172, 254, 0.12), transparent 55%),
      linear-gradient(135deg, #0a0e27 0%, #0f1435 40%, #0a1128 100%);
  }

  .grid-overlay {
    position: absolute;
    inset: 0;
    pointer-events: none;
    opacity: 0.08;
    background-image:
      linear-gradient(rgba(99, 102, 241, 0.1) 1px, transparent 1px),
      linear-gradient(90deg, rgba(99, 102, 241, 0.1) 1px, transparent 1px);
    background-size: 50px 50px;
    animation: grid-move 20s linear infinite;
  }

  @@keyframes grid-move {
    0% { background-position: 0 0; }
    100% { background-position: 50px 50px; }
  }

  .orb {
    position: absolute;
    border-radius: 999px;
    filter: blur(80px);
    opacity: 0.5;
    animation: floaty 20s ease-in-out infinite;
    pointer-events: none;
  }

  .orb.a { 
    width: 500px; 
    height: 500px; 
    left: -250px; 
    top: -250px; 
    background: radial-gradient(circle, rgba(99, 102, 241, 0.15) 0%, transparent 70%);
  }
  
  .orb.b { 
    width: 400px; 
    height: 400px; 
    right: -200px; 
    bottom: -200px; 
    background: radial-gradient(circle, rgba(139, 92, 246, 0.1) 0%, transparent 70%);
    animation-delay: 5s; 
  }
  
  .orb.c { 
    width: 350px; 
    height: 350px; 
    top: 50%; 
    left: 50%; 
    transform: translate(-50%, -50%);
    background: radial-gradient(circle, rgba(79, 172, 254, 0.08) 0%, transparent 70%);
    animation-delay: 10s; 
  }

  @@keyframes floaty {
    0%, 100% { transform: translate(0, 0); }
    33% { transform: translate(30px, -30px); }
    66% { transform: translate(-20px, 20px); }
  }

  .content-wrapper {
    position: relative;
    z-index: 5;
    max-width: 1400px;
    width: 100%;
    margin: 0 auto;
    flex: 1;
    min-height: 0;
    display: flex;
    flex-direction: column;
    gap: 1.2rem;
  }

  .page-header {
    display: flex;
    align-items: center;
    gap: 0.9rem;
    flex-shrink: 0;
    padding: 0.2rem 0;
  }

  .header-badge {
    width: 52px;
    height: 52px;
    border-radius: 16px;
    display: grid;
    place-items: center;
    background: linear-gradient(135deg, #6366f1, #8b5cf6);
    box-shadow: 0 0 20px rgba(99, 102, 241, 0.5);
    border: 1px solid rgba(255, 255, 255, 0.1);
    animation: icon-pulse 2s ease-in-out infinite;
  }

  @@keyframes icon-pulse {
    0%, 100% { 
      transform: scale(1); 
      box-shadow: 0 0 20px rgba(99, 102, 241, 0.5); 
    }
    50% { 
      transform: scale(1.05); 
      box-shadow: 0 0 30px rgba(99, 102, 241, 0.8); 
    }
  }

  .header-badge span {
    font-size: 1.6rem;
    filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.4));
  }

  .page-header h1 {
    margin: 0;
    color: #e5e7eb;
    font-size: 2rem;
    font-weight: 850;
    letter-spacing: 0.2px;
    text-shadow: 0 2px 20px rgba(99, 102, 241, 0.5);
  }

  .contact-container {
    flex: 1;
    min-height: 0;
    overflow: auto;
    padding: 0.8rem 0;
  }

  .contact-container::-webkit-scrollbar { width: 8px; }
  .contact-container::-webkit-scrollbar-thumb { 
    background: linear-gradient(135deg, #6366f1, #8b5cf6); 
    border-radius: 10px; 
  }
  .contact-container::-webkit-scrollbar-track { 
    background: rgba(255, 255, 255, 0.05); 
    border-radius: 10px; 
  }

  .contact-card {
    border-radius: 28px;
    overflow: hidden;
    background: rgba(255, 255, 255, 0.02);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(99, 102, 241, 0.2);
    box-shadow:
      0 20px 60px rgba(0, 0, 0, 0.5),
      inset 0 1px 0 rgba(255, 255, 255, 0.05);
    padding: 2.5rem;
    animation: slideUp 0.6s ease forwards;
    position: relative;
  }

  @@keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .contact-card::before {
    content: '';
    position: absolute;
    top: -2px;
    left: -2px;
    right: -2px;
    bottom: -2px;
    background: linear-gradient(45deg, #6366f1, #8b5cf6, #ec4899, #4facfe);
    border-radius: 28px;
    opacity: 0;
    z-index: 0;
    transition: opacity 0.4s ease;
    background-size: 400% 400%;
    animation: gradient-rotate 8s ease infinite;
  }

  @@keyframes gradient-rotate {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }

  .contact-card:hover::before {
    opacity: 0.5;
  }

  .contact-content {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    align-items: start;
  }

  .info-section {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .info-item {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(99, 102, 241, 0.2);
    border-radius: 16px;
    padding: 1.25rem;
    transition: all 0.3s ease;
  }

  .info-item:hover {
    background: rgba(255, 255, 255, 0.05);
    border-color: rgba(99, 102, 241, 0.4);
    transform: translateX(5px);
  }

  .info-label {
    color: rgba(255, 255, 255, 0.6);
    font-size: 0.85rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-bottom: 0.5rem;
  }

  .info-value {
    color: #e5e7eb;
    font-size: 1.05rem;
    font-weight: 500;
    line-height: 1.6;
  }

  .map-section {
    border-radius: 20px;
    overflow: hidden;
    border: 1px solid rgba(99, 102, 241, 0.2);
    box-shadow: 0 4px 16px rgba(168, 192, 255, 0.2);
    height: 100%;
    min-height: 400px;
  }

  .map-section iframe {
    width: 100%;
    height: 100%;
    min-height: 400px;
    border: none;
    display: block;
  }

  .action-buttons {
    grid-column: 1 / -1;
    display: flex;
    justify-content: center;
    margin-top: 1rem;
    gap: 1rem;
  }

  .btn-edit {
    padding: 0.95rem 2rem;
    border: none;
    border-radius: 16px;
    font-weight: 700;
    font-size: 1rem;
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    color: #fff;
    background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
    box-shadow: 0 8px 25px rgba(245, 158, 11, 0.4);
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    position: relative;
    overflow: hidden;
  }

  .btn-edit::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 0;
    height: 0;
    background: rgba(255, 255, 255, 0.2);
    border-radius: 50%;
    transform: translate(-50%, -50%);
    transition: width 0.5s, height 0.5s;
  }

  .btn-edit:hover::before {
    width: 300px;
    height: 300px;
  }

  .btn-edit:hover {
    transform: translateY(-3px);
    box-shadow: 0 14px 35px rgba(245, 158, 11, 0.5);
  }

  .btn-edit:active {
    transform: translateY(-1px);
  }

  .btn-edit span {
    position: relative;
    z-index: 1;
  }

  .empty-state {
    text-align: center;
    padding: 4rem 2rem;
    color: #9ca3af;
  }

  .empty-icon {
    font-size: 4rem;
    margin-bottom: 1rem;
    opacity: 0.3;
  }

  @@media (max-width: 1024px) {
    .contact-content {
      grid-template-columns: 1fr;
    }

    .map-section {
      min-height: 300px;
    }
  }

  @@media (max-width: 768px) {
    .ai-contact-container {
      padding: 1rem;
    }

    .page-header h1 {
      font-size: 1.6rem;
    }

    .contact-card {
      padding: 1.5rem;
    }
  }
</style>

<div class="ai-contact-container">
  <div class="orb a"></div>
  <div class="orb b"></div>
  <div class="orb c"></div>
  <div class="grid-overlay"></div>

  <div class="content-wrapper">
    <div class="page-header">
      <div class="header-badge"><span>📧</span></div>
      <h1>İletişim Bilgileri</h1>
    </div>

    <div class="contact-container">
      @if (Model != null && Model.Any())
      {
        @foreach (var item in Model)
        {
          <div class="contact-card">
            <div class="contact-content">
              <div class="info-section">
                <div class="info-item">
                  <div class="info-label">📍 Adres</div>
                  <div class="info-value">@item.Address</div>
                </div>

                <div class="info-item">
                  <div class="info-label">📞 Telefon</div>
                  <div class="info-value">@item.CallUs</div>
                </div>

                <div class="info-item">
                  <div class="info-label">✉️ E-posta</div>
                  <div class="info-value">@item.EmailUs</div>
                </div>

                <div class="info-item">
                  <div class="info-label">🕐 Çalışma Saatleri</div>
                  <div class="info-value">@item.OpeningHours</div>
                </div>
              </div>

              <div class="map-section">
                <iframe src="@item.MapLocation" 
                        allowfullscreen="" 
                        loading="lazy" 
                        referrerpolicy="no-referrer-when-downgrade">
                </iframe>
              </div>

              <div class="action-buttons">
                <a href="@Url.Action("UpdateFooterArea", "Footer", new { id = item.FooterId })" class="btn-edit">
                  <span>✏️ Düzenle</span>
                </a>
              </div>
            </div>
          </div>
        }
      }
      else
      {
        <div class="contact-card">
          <div class="empty-state">
            <div class="empty-icon">📧</div>
            <h3>Henüz iletişim bilgisi eklenmemiş</h3>
            <p>İletişim bilgilerinizi ekleyerek başlayın</p>
          </div>
        </div>
      }
    </div>
  </div>
</div>